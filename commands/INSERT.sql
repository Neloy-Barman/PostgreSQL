-- Way 1
-- INSERT INTO public.users(id, name)
-- VALUES(1, 'Rolf Smith');

-- SELECT * FROM users;

-- Way 2
-- INSERT INTO public.users
-- VALUES(2, 'Michael Smith');

-- INSERT INTO public.videos(id, name, user_id)
-- VALUES(5, 'This is it', 2);

-- INSERT INTO public.videos VALUES(6, 'This is that', 2);
-- INSERT INTO public.videos VALUES(7, 'Hello Fucking world!', 2);

-- SELECT * FROM videos;
